config BR2_PACKAGE_MICRONUX_ISOLATION_TEST
	bool "micronux-isolation-test"
	depends on !BR2_USE_MMU
	help
	  Hardware-only bFLT probe for the MicroNUX reserved user pool and
	  U-visible VMA ownership contract.
