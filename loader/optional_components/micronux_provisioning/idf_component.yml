dependencies:
  espressif/esp_hosted:
    version: "=2.11.5"
    rules:
      - if: "target == esp32p4"
  espressif/esp_wifi_remote:
    version: "=1.3.1"
    rules:
      - if: "target == esp32p4"
  espressif/network_provisioning:
    version: "=1.2.4"
    rules:
      - if: "target == esp32p4"
  espressif/qrcode:
    version: "=0.1.0~2"
    rules:
      - if: "target == esp32p4"
